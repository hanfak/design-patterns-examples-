public class AlarmNotifier {
    // Connect to google calendar, tokens as env var https://developers.google.com/calendar/api/guides/overview
    // For all alarms generated, send request to calendar to add to calendar
    // for testing can have stub service which is running on thread, which checks the alarms, compares with date time
    //      and rings alarm and store alarms rang in a datastore (hashmap)or log file

}
